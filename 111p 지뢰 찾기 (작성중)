#include <iostream>
#include <time.h>

using namespace std;

int main()
{
    //2차원의 게임판 안에 지뢰가 숨겨져 있고 이 지뢰를 모두 찾아내는 게임
    // //지뢰가 없는 곳을 클릭했을 때 숫자가 나오면 주변칸에 지뢰가 숨겨져 있다는 것을 의미한다.
    //예를 들어 숫자가 2면 주변칸에 지뢰가 두 개 있다는 의미가 됨.

    //10x10 크기의 2차원 배열 생성
    //이 배열에 지뢰를 숨김
    // 지뢰 아닌 곳은 .으로 표시, 지뢰인 곳은 #으로 표시
    // 어떤 칸 지뢰일 확률은 난수 발생시켜 결정. 전체 30% 지뢰 설정
    //0부터 99사이의 난수 생성해 30보다 적은 경우에만 지뢰 놓기
    srand(time(NULL));
    char list[10][10];
    int RandomValue;
    for (int i = 0; i < 10; i++)
    {
        RandomValue = rand() % 100;
        for (int j = 0; j < 10; j++) {
            if (RandomValue < 30) {

            }
        }
        cout << endl;
    }




}

